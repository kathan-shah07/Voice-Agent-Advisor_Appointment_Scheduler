# Dependencies
node_modules/
yarn.lock
# Note: package-lock.json should be committed for consistent installs

# Environment variables
.env
.env.local
.env.*.local
.env.test
.env.production

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
# Application logs (if any)
app.log
error.log
combined.log

# Testing
coverage/
.nyc_output/
*.lcov
.jest/

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Build and output directories
dist/
build/
out/
*.tsbuildinfo

# Temporary files
tmp/
temp/
*.tmp
.cache/

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# Parcel-bundler cache
.parcel-cache

# Next.js (if used in future)
.next/

# Nuxt.js (if used in future)
.nuxt/

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# Voice/audio logs (if any)
voice_logs/
audio_logs/

# Note: req.txt, test-*.js, and *.md files are intentionally NOT ignored
# as they are part of the project documentation and test scripts

# Google Service Account Keys and Credentials (Phase 2: MCP Integration)
credentials/
tokens/
*.json
!package.json
!package-lock.json
!tsconfig.json
!jest.config.js
!templates/*.json
service-account*.json
*-credentials.json
*-key.json

# Data directory (runtime data)
data/

# MCP submodule build artifacts (keep source, ignore dist)
mcp-gmail-calendar/dist/
mcp-gmail-calendar/node_modules/
mcp-gmail-calendar/logs/
mcp-gmail-calendar/tokens/
mcp-google-sheets/dist/
mcp-google-sheets/node_modules/
mcp-google-sheets/logs/

